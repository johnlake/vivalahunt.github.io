document.addEventListener('DOMContentLoaded', () => {
  const button = document.getElementById('playButton');

  if (button) {
    button.addEventListener('click', () => {
      alert('Play action to be implemented.');
    });
  }
});
